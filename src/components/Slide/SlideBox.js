import styled from '@emotion/styled';
import React from 'react';

const SlideBoxContainer = styled.div``;

const SlideBox = ({ ...props }) => {
  return <SlideBoxContainer {...props}></SlideBoxContainer>;
};

export default SlideBox;
